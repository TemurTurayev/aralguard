# Скопировать в .env и заполнить. .env в git НЕ попадает.
ADS_API_KEY=            # ads.atmosphere.copernicus.eu -> профиль
CDS_API_KEY=            # cds.climate.copernicus.eu -> профиль
EARTHDATA_TOKEN=        # urs.earthdata.nasa.gov -> Generate Token
GEE_PROJECT=            # id проекта Earth Engine (например aralguard-mvp)
TELEGRAM_BOT_TOKEN=     # @BotFather -> /newbot
